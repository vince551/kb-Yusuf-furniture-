'use client';
import Link from 'next/link';
import { ArrowLeft, ShoppingBag } from 'lucide-react';
import { useEffect, useState } from 'react';
export default function Cart(){const [count,setCount]=useState(0);useEffect(()=>setCount(Number(localStorage.getItem('tovi_cart_count')||0)),[]);return <main className="site-shell min-h-screen"><header className="site-header"><Link href="/" className="brand"><span>VINCE</span><small>Maison · Shopping Bag</small></Link><Link href="/shop" className="bag-link">Continue shopping →</Link></header><section className="editorial-intro"><div><p className="eyebrow">YOUR BAG</p></div><div><h1 className="display" style={{fontSize:'clamp(4rem,9vw,8rem)',margin:0}}>Shopping<br/><i>bag.</i></h1><p>{count ? `${count} piece${count===1?'':'s'} selected.` : 'Your considered edit starts here.'}</p></div></section><section style={{padding:'90px 7vw'}}>{count===0?<div style={{textAlign:'center',padding:'80px 20px',borderTop:'1px solid var(--line)'}}><ShoppingBag size={36} style={{color:'var(--gold)'}}/><h2 className="display" style={{fontSize:'3rem'}}>Your bag is beautifully empty.</h2><p style={{color:'var(--muted)'}}>Explore the collection and add a piece when something feels right.</p><Link href="/shop" className="btn-gold">Explore collection</Link></div>:<div style={{maxWidth:900,margin:'auto'}}><h2 className="display" style={{fontSize:'3rem'}}>Ready for checkout.</h2><p style={{color:'var(--muted)',lineHeight:1.8}}>Your cart is stored locally for now. Supabase checkout, live stock and secure payments will be connected in the next commerce layer.</p><Link href="/shop" className="gold-link"><ArrowLeft size={14}/> Add more pieces</Link></div>}</section></main>}
