'use client';
import {FormEvent,useState} from 'react';
export default function Concierge(){const [message,setMessage]=useState('');const [reply,setReply]=useState('');const [loading,setLoading]=useState(false);async function send(e:FormEvent){e.preventDefault();setLoading(true);setReply('');const r=await fetch('/api/concierge',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message})});const d=await r.json();setReply(d.reply);setLoading(false)}return <main className="min-h-screen bg-ink px-6 py-16 md:px-20"><a href="/" className="display text-xl tracking-[.2em] text-champagne">VINCE</a><div className="mx-auto mt-24 max-w-3xl"><p className="text-[10px] uppercase tracking-[.3em] text-champagne">VINCE AI · PRIVATE CONCIERGE</p><h1 className="display mt-5 text-5xl md:text-7xl">Tell us how you<br/><i className="text-champagne">want to live.</i></h1><p className="mt-6 max-w-xl text-sm leading-7 text-stone-500">Describe your room, style, dimensions and budget. Our concierge will help you shape the collection.</p><form onSubmit={send} className="mt-12"><textarea value={message} onChange={e=>setMessage(e.target.value)} required rows={5} placeholder="e.g. I need a warm, modern living room for a small apartment under KSh 150,000..." className="w-full resize-none border border-white/10 bg-[#1c1a17] p-5 text-sm text-white outline-none focus:border-champagne"/><button disabled={loading} className="mt-4 bg-champagne px-7 py-4 text-[10px] font-semibold uppercase tracking-[.18em] text-ink">{loading?'Thinking…':'Ask the concierge →'}</button></form>{reply&&<div className="mt-10 border border-white/10 bg-[#1b1916] p-6 text-sm leading-7 text-stone-300"><p className="mb-3 text-[9px] uppercase tracking-[.2em] text-champagne">VINCE Concierge</p>{reply}</div>}</div></main>}