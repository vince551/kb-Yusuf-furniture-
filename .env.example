# Appwrite Cloud
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_PROJECT_ID=kb-yusuf-furniture

# Optional: only needed if you later move quotation history to Appwrite Databases.
# The current customer quotation system stores requests in each customer's Appwrite account preferences.

# Gemini AI — server only, never expose in client code
GEMINI_API_KEY=
