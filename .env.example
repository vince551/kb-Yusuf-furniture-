# Appwrite Cloud
NEXT_PUBLIC_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
NEXT_PUBLIC_APPWRITE_PROJECT_ID=

# Appwrite database IDs (add when the quotation database is created)
NEXT_PUBLIC_APPWRITE_DATABASE_ID=
NEXT_PUBLIC_APPWRITE_QUOTES_COLLECTION_ID=

# Gemini AI — server only, never expose in client code
GEMINI_API_KEY=
